# Changelog

## 2026-06-24 15:15 EDT - add .gitignore and remove committed .DS_Store files

- Added a `.gitignore` covering macOS `.DS_Store` files, common editor folders, logs, and local Claude config.
- Removed three `.DS_Store` files that were previously committed to the repo.
- `CLAUDE.md` and `.claude/` are now ignored so local config stays out of the public repo.
